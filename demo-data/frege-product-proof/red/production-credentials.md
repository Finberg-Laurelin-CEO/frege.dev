# Production credential handling

This restricted fixture exists only to prove that a green-zone agent cannot
retrieve matching sensitive material. It contains no usable credential,
provider name, account identifier, or operational procedure.

The title, body, tags, and snippets must remain absent from a denied context
packet.
